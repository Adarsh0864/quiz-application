spring.application.name=Quiz

# MongoDB (uses env variable MONGODB_URI in production, falls back to Atlas for dev)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://am9774981:hmR9q9mX7pwTn4cx@cluster0.93k4d.mongodb.net/quizdb?appName=Cluster0}

# Server
server.port=${PORT:8080}

# Swagger UI path
springdoc.swagger-ui.path=/swagger-ui.html

# CORS
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
